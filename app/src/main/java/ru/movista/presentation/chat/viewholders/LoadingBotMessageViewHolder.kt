package ru.movista.presentation.chat.viewholders

import android.view.View
import ru.movista.presentation.chat.LoadingBotMessage

class LoadingBotMessageViewHolder(view: View) : androidx.recyclerview.widget.RecyclerView.ViewHolder(view) {

    fun bind(loadingBotMessage: LoadingBotMessage) {
        // stub
    }
}